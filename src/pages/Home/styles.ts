
import { makeStyles } from '@material-ui/styles'

const style = makeStyles(() => ({
  Container: {
    padding: '0 60px',
    height: '15.2rem',
     width: '35%',
  },

  Title: {
 
    textAlign: 'center',
    marginBottom: 30,
  },
}))

export default style;